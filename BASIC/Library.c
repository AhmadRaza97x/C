// LIBRARY FUNTION

#include<stdio.h>

    // print("");
    // scanf("");

#include<math.h>

    // pow(a,b);  ISKA MTLB A KI POWER B
    // sqrt(a);   ISKA MTLB A KA SQUARE ROOT
    // cbrt(a);   A KA CUBE ROOT

#include<limits.h>

    // INT_MIN    IT STORE MIN VALUE OF INT (-2 KI POWER 32)
    // INT_MAX    IT STORE MAX VALUE OF INT (2 KI POWER 32)

#include<stdbool.h>

    // bool a = true/false  IT STORE ONLY TRUE OR FALSE / 0 OR 1

#include<string.h>

    // %s                   TO PRINT STRING (format specifier)
    // strlen(char*str);    RETURN SIZE/LENGTH OF STRING
    // strcpy(char*str1,char*str2)  COPY CONTENT OF STR2 TO STR1 & DEEP COPY
    // strcat(char *str1,char *str2) CONCATENATE
    // strcmp
